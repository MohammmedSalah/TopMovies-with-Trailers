<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.salah.mohammed.topmoviewtihtrailer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    android:configChanges="keyboard|keyboardHidden|orientation"

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.salah.mohammed.topmoviewtihtrailer.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.salah.mohammed.topmoviewtihtrailer.activities.DetailActivity" />
        <activity android:name="com.salah.mohammed.topmoviewtihtrailer.activities.ReviewActivity" />
        <activity android:name="com.salah.mohammed.topmoviewtihtrailer.activities.TrailerActivity"></activity>
    </application>

</manifest>